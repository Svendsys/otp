# Development and CI dependencies.
#
# The print unit itself installs everything from apt (see device/packages.txt)
# so that a freshly flashed image needs no network on first boot. This file is
# only for running the test suite on a development machine.
reportlab
pytest
pypdf
pyyaml

# The panel's pixels. tests/test_panel_pixels.py drives the real
# Ssd1306Display drawing code through luma's `dummy` device, which renders
# to a PIL image -- so the 128x64 layout is asserted without an I2C bus.
# luma.core, not luma.oled: the ssd1306 class is only touched by the
# constructor the test bypasses, and this pulls in Pillow, which is the
# part that actually matters.
luma.core
