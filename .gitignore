__pycache__/
*.pyc

# Generated pads are key material — never commit them
output/
pads/

# Rendered at image-build time by image/render-manual.sh
assets/*.pdf

# pi-gen checkout and build output
image/pi-gen/
image/deploy/
*.img
*.img.xz
*.deb

# Isolated git worktrees created for subagents. Full checkouts of
# this repository -- 3.2MB each -- and pure tooling scratch. One got as
# far as being offered for commit before this line existed.
.claude/worktrees/
